var number;
var otherNumber;
var operacao;
function calculadoraAdicaoSubtracao (number, otherNumber, operacao) {
   for (var adicao = 0; adicao > 0; adicao++) {
       if (operacao == "+") {
           return adicao = number + otherNumber;
       }
   for (var subtracao = 0; subtracao > 0; subtracao++) {
       if (operacao == "-") {
           return subtracao = number - otherNumber;
       } else { 
           return 0;
       }
   }
   }
}